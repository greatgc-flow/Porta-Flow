{
  "schema_version": 1,
  "categories": {
    "shell-dialect": {
      "description": "Shell syntax and environment variable rules",
      "examples": ["PowerShell vs bash syntax", "env var assignment", "pipeline operators"]
    },
    "encoding": {
      "description": "File encoding and BOM rules",
      "examples": ["UTF-8 without BOM", "avoid UTF-16"]
    },
    "path-portability": {
      "description": "Portable path construction rules",
      "examples": ["no drive-letter hardcoding", "use pathlib/_sys-relative"]
    },
    "tool-interface": {
      "description": "Preferred tool choices for given operations",
      "examples": ["Grep over grep/rg", "Write over echo/cat", "Glob over find"]
    },
    "context-cold-start": {
      "description": "Correct behavior when starting without prior context",
      "examples": ["do not assume prior state", "read handoff.md before acting"]
    },
    "governance-drift": {
      "description": "Rules about authority boundaries and consensus requirements",
      "examples": ["quorum unclear → NEED_MORE_INFO", "no binding decision without FINALIZED"]
    },
    "directive-boundary": {
      "description": "Rules about what belongs in which store (user-directives vs lessons vs runtime)",
      "examples": ["peer operational lessons must not go into user-directives.md"]
    }
  },
  "severity_levels": ["critical", "high", "medium", "low"],
  "scope_values": ["global", "workspace"],
  "status_values": ["candidate", "active", "retired", "rejected", "superseded"]
}
